package api

const(
	// URL Pathing
	TARGET_URL = "maprando.com"
	DEV_PREFIX = "dev."

	RANDOMIZE = "/randomize"
	UNLOCK = "/randomize/unlock"
)

const(
	// Map Rando CLI
	SPOILER_TOKEN = "spoiler_token"
	SETTINGS = "settings"
)

const(
	// Defined presets
	SEASON4 = "Community Races Season 4"
	SEASON5 = "Community Races Season 5"
	MENTOR = "Mentor Tournament"
	DEFAULT = "Default"
	
	JSON_SUFFIX = ".json"
)

const(
	// Setting names
	PRESET_SETTING_NAME = "name"
	RACE_SETTING = "Race Mode"
)